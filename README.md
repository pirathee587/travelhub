# Admin Portal — Frontend Scaffold

This is a frontend-only scaffold for the Admin Portal UI (React + Vite).

Quick start (after installing dependencies):

```bash
npm install
npm run dev
```

The browser tab title and package name are set to "Admin Portal".

Replace mock/UI placeholders with real components and assets as needed.
