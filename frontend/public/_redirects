# Proxy /api/* to EC2 backend (avoids browser mixed-content: Netlify calls HTTP, not the browser)
/api/*  http://18.140.197.169:8080/api/:splat  200

# SPA fallback — must come AFTER the API proxy rule
/*    /index.html   200
